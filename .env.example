# Supabase (catalogue, collabs, commandes, storage des licences/audio)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Webhook Google Apps Script — checkout (crée une session de paiement, ex. Stripe)
# Le script doit accepter un POST JSON { items, email } et répondre { url } (lien de paiement)
VITE_CHECKOUT_WEBHOOK_URL=

# Webhook Google Apps Script — formulaire de contact / booking
# Le script doit accepter un POST JSON { name, email, subject, message } et renvoyer { ok: true }
VITE_CONTACT_WEBHOOK_URL=

# Réseaux
VITE_INSTAGRAM_HANDLE=__naifos
VITE_GENIUS_URL=https://genius.com/artists/Naifos
